# Dependencies for format-book-agent (PDF and EPUB)
markdown>=3.4
weasyprint>=62.0  # PDF from HTML/CSS with full control of @page, margins, counters
ebooklib>=0.18
pypdf>=4.0  # Inject title, author, subject, keywords into PDF after build
